package com.example.test

enum Color { RED, GREEN, BLUE }

struct Point {
    x: Double
    y: Double
}

interface Listener {
    fun onUpdate(value: String)
}

class Calculator {
    constructor create()
    fun add(a: Int, b: Int): Int
    property lastResult: Int
}

exception MyError(String)
