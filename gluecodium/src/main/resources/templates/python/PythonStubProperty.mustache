{{! Python property stub (getter + optional setter). }}
    @property
    def {{resolveName}}(self){{#unless typeRef.isNullable}} -> {{resolveName typeRef}}{{/unless}}: ...

{{#if hasSetter}}
    @{{resolveName}}.setter
    def {{resolveName}}(self, value{{#unless typeRef.isNullable}}: {{resolveName typeRef}}{{/unless}}) -> None: ...
{{/if}}
